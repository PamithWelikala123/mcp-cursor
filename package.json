{
  "name": "xapihub-mcp-extension",
  "version": "1.0.0",
  "description": "Cursor MCP extension for XAPIHub integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "jest",
    "test-connection": "tsc && node dist/test/test-connection.js",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "setup": "node setup.js"
  },
  "keywords": ["mcp", "cursor", "xapihub", "api"],
  "author": "XAPIHub Team",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}